# Capacity Agent 领域语言

本上下文描述性能容量治理平台中的业务概念。平台以 Capacity Agent 为统一用户角色，将容量数据转化为可追溯、可执行、可验证的治理闭环。

## 资源与数据

**系统**：
被平台治理且具有独立负责人、重要性级别和组件拓扑的业务系统。
_避免使用_：应用、项目（除非特指系统内的应用组件或项目管理对象）

**系统字典**：
系统名称与简称必须使用统一登记值：S支付平台（PAY）、S新核心_账户处理（ACT）、S新核心_授权交易（AUTH）、S新核心_数据&应用服务（DASP）、S实时数据服务（DSI）、S数据服务网关（DPS）、S账单打印系统（BPC）、S用户标签数据（LMP）、S客户信息治理系统（CIMS）。
_避免使用_：A 系统、B 系统、统一支付系统、实时风控系统、渠道接入平台、数据中台等演示或历史别名

**组件类型**：
具有共同技术语义的一类组件，如 GREATDB、NGINX、REDIS 或应用。
_避免使用_：模块（`module` 仅保留为外部接口字段）

**逻辑资源**：
系统内承担一个可识别职责、由一个或多个服务器实例组成的集群级部署单元。
_避免使用_：组件、系统、单台服务器实例

**服务器实例**：
可通过稳定资源标识、主机名或 IP 识别，并承载逻辑资源的计算节点。
_避免使用_：服务器、主机、节点混用

**系统画像**：
描述“系统—组件类型—逻辑资源（集群）—服务器实例”四级归属关系的架构上下文快照。多个逻辑资源在同一组件类型下是并列关系；集群节点分布、集群级归因与建议方案归属于逻辑资源，单台服务器的动态基线和容量预测归属于服务器实例。
_避免使用_：CMDB 数据（系统画像可以来源于 CMDB，但两者不等价）

**治理地图**：
按用户、平台和职能视角组织系统字典的全景导航，用于确定治理覆盖范围并进入对应系统画像。
_避免使用_：系统拓扑（治理地图描述业务视角分类，系统拓扑描述资源归属关系）

**性能样本**：
某个服务器实例、某项指标在一个确定时间点或时间窗口内的度量值。
_避免使用_：指标（指标是度量定义，样本是具体值）

**昨日汇总**：
数据源对昨日 CPU、内存、磁盘利用率计算出的平均值、峰值和最小值集合。
_避免使用_：日报（日报包含分析结论，昨日汇总只是输入数据）

**证据快照**：
一次分析所使用的原始接口响应、标准化数据和派生特征的不可变记录。
_避免使用_：日志、附件

## 分析与治理

**基线状态**：
数据源或确定性规则针对单项性能数据给出的初步偏离状态，不包含架构语义判断。
_避免使用_：最终异常、健康状态

**分析发现**：
Capacity Agent 对一个明确资源范围给出的、带数据证据和置信度的判定结果。
_避免使用_：告警、JIRA

**确认异常**：
同时满足确定性异常策略和最低证据要求，需要进入治理流程的分析发现。
_避免使用_：AI 猜测、单次阈值越界

**预期偏离**：
偏离通用基线但符合主备、灾备或其他已知架构角色预期的分析发现。
_避免使用_：误报、已恢复

**优化机会**：
不存在即时故障风险，但有证据表明可以减少闲置、规格冗余或资源不均的分析发现。
_避免使用_：低利用率异常、确认异常

**治理建议**：
针对分析发现生成的、引用数据证据与已发布知识依据的可执行处置方案。
_避免使用_：模型回答、治理结果

**治理事项**：
从分析发现开始，串联建议、JIRA、执行、观察和验证的内部闭环记录。
_避免使用_：JIRA 单、任务（JIRA 只是外部执行载体）

**观察周期**：
治理执行完成后，按治理类型配置的持续采样和效果评估时间范围。
_避免使用_：等待时间

**效果验证**：
将治理后的指标与治理前基线及验收条件比较，并判断治理是否有效的过程。
_避免使用_：JIRA 完成、人工关闭

## Agent、知识与记忆

**Capacity Agent**：
面向用户的统一容量治理角色，持续巡检、解释、行动和跟进；内部由若干能力模块协作完成工作。
_避免使用_：聊天机器人、多 Agent UI

**治理知识**：
经过版本化、审核和发布的性能容量管理办法、异常规则补充和处置手册。
_避免使用_：模型记忆、当前接口数据

**Agent 记忆**：
从历史对话、治理案例和验证结果中沉淀，具有来源、置信度和有效期的可复用上下文。
_避免使用_：知识库、聊天记录

**工作事件**：
Capacity Agent 自主执行取数、分析、建单、跟进和验证时产生的用户可见进度记录。
_避免使用_：系统日志
